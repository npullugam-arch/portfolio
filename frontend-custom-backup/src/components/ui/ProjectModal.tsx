import {AnimatePresence,motion} from 'framer-motion'; import {projects} from '../../data/portfolio'; import {useApp} from '../../store';
export function ProjectModal(){const {project,setProject}=useApp();const p=project===null?null:projects[project];return <AnimatePresence>{p&&<motion.div className="modal-backdrop" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onMouseDown={()=>setProject(null)}><motion.article className="project-modal" initial={{y:'100%'}} animate={{y:0}} exit={{y:'100%'}} transition={{duration:.7,ease:[.22,1,.36,1]}} onMouseDown={e=>e.stopPropagation()}><button className="close" onClick={()=>setProject(null)} aria-label="Close project">×</button><span style={{color:p.accent}}>CASE STUDY / 0{project!+1}</span><h2>{p.title}</h2><p>{p.tagline}</p><h3>Core features</h3><ul>{p.features.map(x=><li key={x}>{x}</li>)}</ul><div className="tags">{p.technologies.map(x=><span key={x}>{x}</span>)}</div><footer><a href={p.github}>GITHUB ↗</a><a href={p.demo}>LIVE DEMO ↗</a></footer></motion.article></motion.div>}</AnimatePresence>}
