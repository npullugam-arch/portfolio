import {useApp} from '../../store'; export function Navigation(){const {section,setSection}=useApp();return <header className={`topbar ${section==='about'?'dark':''}`}><button className="wordmark" onClick={()=>setSection('home')} aria-label="Home">M◇</button><nav>{(['about','projects','contact'] as const).map(id=><button key={id} className={section===id?'active':''} onClick={()=>setSection(id)}>{id.toUpperCase()}</button>)}</nav><div className="nav-actions"><button className="touch" onClick={()=>setSection('contact')}>GET IN TOUCH</button><button className="sound" aria-label="Toggle sound">◖</button></div></header>}
